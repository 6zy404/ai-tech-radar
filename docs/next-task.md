# Next Task

这份文件只回答两个问题：**现在是什么状态**，**接下来还有什么没做**。它是一块
状态板，不是日志——每次改动时**替换**内容，不要往上追加。

- 每一轮做了什么、怎么量的、为什么这样决定：写进 `CHANGELOG.md` 的条目。
- 2026-07-10 至 2026-09-24 的 110 条会话记录原样归档在
  [`docs/history/next-task-log.md`](history/next-task-log.md)，只读，需要
  查一个旧结论的原话时去 grep。
- 「现在有什么功能」看 `docs/reference.md`，不在这里重复。

## 当前状态（2026-09-26）

- **线上**：`https://aizyradar.cn`，Cloudflare Tunnel 接本机 `next start`
  （公开构建 `npm run build:public`，工作台路由物理不存在）。跑的是 `main`
  的 `df51cc4`，2026-09-25 部署；此前一天完成了全项目体检、P0 止血、P1 读者
  可见问题与 P2 前三项（模型加载期限、代理探测直连回退、`GET /api/health`）。
- **部署方式**：旁路构建后换目录重启，约十秒停机，步骤在
  `docs/deployment.md` →「Rebuild with seconds of downtime」。主检出里的
  `.next-old` 是上次部署的回滚目录，owner 决定先留着，下次部署后再删。
- **编辑轮怎么跑**：线上没有工作台，一轮在临时 `next dev` 上做，指向线上
  `config/`（`docs/editorial-round-playbook.md` Step 0）。上一轮 2026-09-24，
  候选池当时清零；定时导入每天 08:05 自动跑并自动拒掉预发布 tag 与已发布过
  的条目，其余候选**在处置前都公开挂在快讯页上**。
- **内容**（2026-09-26 经公开 getter 实测，与 README 一致）：已发布信号 81、
  技能 16、知识 19、技术简报 30 期（最近 09-24）、在用信源 13；图谱 116 节点 /
  802 边，兜底「关联」占 12.7%。技能/知识池零占位。
- **闸门**：typecheck / lint / format / vitest 327 / `validate:all` 22 个全绿
  （2026-09-25）。CI 跑单测与全部 validator。
- **每个新会话自动读入的文档**：约 270 KB（2026-09-26 起）。此前 CHANGELOG 与
  本文件的历史合计 800 KB 也在自动导入里，现已分别改为按需读取与归档。

## 剩余待办（按优先级）

### 1. 编辑轮（周期性，不是一次性任务）

距上次 09-24。候选每天累积、公开可见，超过二十条或一周未处置就该跑一轮，按
手册走。分诊建议已在轮次控制台上（高置信拒绝可信，发布建议弱）。

### 2. P2 运维剩余

| 项                     | 现状                                                   | 做完的效果                                           | 备注                                        |
| ---------------------- | ------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------- |
| 服务只监听 `127.0.0.1` | `next start` 监听所有网卡；隧道只连 `localhost:3000`   | 局域网够不到服务；限流的客户端标识不能再绕过代理伪造 | 改启动任务加 `-H 127.0.0.1`，重启一次并验证 |
| 部署脚本化             | 手册里是三步手工 PowerShell                            | 一条命令部署、一条命令回滚，含健康检查               | 可顺手做日志轮转                            |
| 日志轮转               | 三个日志共约 100 KB，最大 `task-runner-cron.log` 79 KB | 不紧急                                               | 并进部署脚本                                |
| 异盘备份               | 快照与源同盘（`npm run backup:data`，每天 07:45）      | 盘坏不至于两份全丢                                   | **需要 owner 提供第二块盘或网络位置**       |

### 3. P3 代码与文档债

- `src/app/globals.css` 11,158 行：按页面/组件拆文件，用全站计算样式 diff
  证明零变化（本仓库做过多次，方法在 `docs/design-system.md`）。
- 测试文件不做类型检查；体检提到的大面积复制粘贴——具体位置见归档里
  2026-09-24（evening）那条，动手前先重新量。
- ~~README 数字过期、本文件 4400 行、roadmap/progress 停在七八月~~：2026-09-26
  已做（README 数字实测未变；progress.md 退役，由 reference.md 接替）。

### 4. 评测与内容

- owner 抽查 `eval/search/queries.json`；再由**没看过语料的人**写一套搜索与
  问雷达的问题集（含一半答不出来的），两套评测的数字才算数。
- 分诊提示词 v2：在 2026-09-23 之后的决定上评判，别在旧测试集上调。理由码
  显示修法是**把文章给模型**，不是更好的评分表。
- 已知不改：390px 下技能《检索流水线调优》标题断成「检索流水线调 / 优」
  （ICU 词典缺口，种子标题被三条正文引用）。

### 5. 卡在 owner 的事

- 仓库改 Public、更新描述（网页操作）。
- 外部拨测：每 5 分钟打 `/api/health`，连续两次失败再告警（账号是 owner 的，
  配置写在 `docs/deployment.md`）。
- 主机夜里被手动休眠时站点跟着下线；`WakeToRun` 只保证 07:45 之后恢复。
- 第二块盘（见异盘备份）。

## 长期不做的

见 `AGENTS.md` →「What is intentionally NOT implemented yet」：账号 / RBAC、
生产数据库、推送与邮件订阅、全站 i18n、黑盒推荐。不要在没有明确要求时加进
待办。

## 怎么维护这份文件

- 完成一项：从待办里删掉，或像上面那样划掉并留一行日期；状态区改成新的事实。
- 新发现一项：写进对应优先级，附现状与做完的效果，两行以内。
- 不要写过程叙述。过程写进 `CHANGELOG.md`，那里有日期、数字与验证方式。
- 目标：这份文件保持在 150 行以内。
